# pipeline-anatomy has no runtime configuration — it is a fully static site.
# The one optional variable below is for MAINTAINERS running the redaction gate locally.

# Secret salt for the HMAC redaction denylist (matches the CI secret).
# Forks can leave this unset: the HMAC half of the redact test skips loudly without it.
REDACT_SALT=
