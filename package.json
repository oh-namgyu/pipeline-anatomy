{
  "name": "pipeline-anatomy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive walkthroughs of three real content-automation pipelines, generalized: scheduled runs, quality gates, failure branches and the design decisions behind them.",
  "license": "MIT",
  "scripts": {
    "serve": "python3 -m http.server 6185 --bind localhost",
    "test": "node --test \"tests/*.test.mjs\"",
    "redact:hmac": "node scripts/redact-hmac.mjs",
    "e2e": "playwright test",
    "e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
